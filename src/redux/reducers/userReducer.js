const initialState = {
    // initial state
  };
  
  const userReducer = (state = initialState, action) => {
    switch (action.type) {
      // handle different actions
      default:
        return state;
    }
  };
  
  export default userReducer;
  